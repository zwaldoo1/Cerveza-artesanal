---
import BaseLayout from "../layout/BaseLayout.astro";

const meta = {
  title: "Conócenos",
  description: "Nuestra historia, equipo y filosofía cervecera",
};
---

<BaseLayout {...meta}>
  <article class="relative">
    <!-- HERO / BRAND AURORA -->
    <section class="relative overflow-hidden rounded-2xl border border-white/10 bg-black/20 px-6 py-14 sm:px-10 sm:py-16 md:py-20">
      <!-- Fondo aurora + grain -->
      <div class="pointer-events-none absolute inset-0 -z-10">
        <div class="absolute inset-0 opacity-90"
             style="background:
               radial-gradient(45% 55% at 12% 20%, rgba(245,158,11,.25), transparent 60%),
               radial-gradient(45% 55% at 88% 25%, rgba(56,189,248,.18), transparent 62%),
               radial-gradient(55% 60% at 72% 80%, rgba(124,58,237,.16), transparent 60%),
               #0b0b0f;">
        </div>
        <div class="absolute inset-0 mix-blend-overlay opacity-40"
             style="background-image:
               repeating-linear-gradient(0deg, rgba(255,255,255,.035) 0 2px, transparent 2px 4px),
               repeating-linear-gradient(90deg, rgba(255,255,255,.035) 0 2px, transparent 2px 4px);">
        </div>
      </div>

      <div class="mx-auto max-w-6xl">
        <span
          class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1 text-xs text-white/90 backdrop-blur hover:bg-white/15 transition">
          <span class="inline-block h-2 w-2 rounded-full bg-amber-400 animate-pulse"></span>
          identidad local · craft • real
        </span>

        <h1 class="mt-5 text-4xl/tight font-semibold tracking-tight text-white sm:text-5xl">
          Conócenos
          <span class="block bg-gradient-to-r from-amber-400 via-fuchsia-400 to-sky-400 bg-clip-text text-transparent">
            La historia detrás de la espuma
          </span>
        </h1>

        <p class="mt-4 max-w-2xl text-white/80">
          Somos una cervecería independiente que mezcla tradición y curiosidad.
          Ingredientes nobles, procesos limpios y una obsesión sana por el detalle.
        </p>

        <!-- KPIs -->
        <div class="mt-10 grid gap-4 sm:grid-cols-3">
          <div class="group rounded-xl border border-white/10 bg-white/10 p-5 text-white/90 backdrop-blur transition hover:scale-[1.01]">
            <div class="text-3xl font-semibold">+50k</div>
            <div class="mt-1 text-sm text-white/70">Botellas al año</div>
          </div>
          <div class="group rounded-xl border border-white/10 bg-white/10 p-5 text-white/90 backdrop-blur transition hover:scale-[1.01]">
            <div class="text-3xl font-semibold">8</div>
            <div class="mt-1 text-sm text-white/70">Estilos permanentes</div>
          </div>
          <div class="group rounded-xl border border-white/10 bg-white/10 p-5 text-white/90 backdrop-blur transition hover:scale-[1.01]">
            <div class="text-3xl font-semibold">4.8★</div>
            <div class="mt-1 text-sm text-white/70">Satisfacción de clientes</div>
          </div>
        </div>
      </div>
    </section>

    <!-- ADN / VALORES -->
    <section class="mx-auto mt-10 max-w-6xl">
      <div class="grid gap-6 md:grid-cols-3">
        <div class="rounded-xl border border-white/10 bg-white/5 p-6 text-white/90 backdrop-blur">
          <h2 class="text-xl font-medium">Nuestra marca</h2>
          <p class="mt-2 text-white/70">
            Nacimos como proyecto de garaje y crecimos con la comunidad.
            Hoy seguimos cocinando con la misma cercanía y respeto por la materia prima.
          </p>
          <div class="mt-4 flex flex-wrap gap-2">
            <span class="chip">Local</span>
            <span class="chip">Pequeños productores</span>
            <span class="chip">Temporadas</span>
          </div>
        </div>

        <div class="rounded-xl border border-white/10 bg-white/5 p-6 text-white/90 backdrop-blur">
          <h2 class="text-xl font-medium">Fundadores</h2>
          <p class="mt-2 text-white/70">
            Equipo curioso y obsesivo: levadura feliz, cliente feliz.
            Nos mueve compartir y experimentar con responsabilidad.
          </p>
          <div class="mt-4 grid grid-cols-2 gap-3">
            <div class="founder">
              <div class="founder-img bg-[url('/images/fundador1.jpg')]"></div>
              <div class="mt-2 text-sm font-medium">Eric</div>
              <div class="text-xs text-white/60">Maestra cervecera</div>
            </div>
            <div class="founder">
              <div class="founder-img bg-[url('/images/fundador2.jpg')]"></div>
              <div class="mt-2 text-sm font-medium">Nicole</div>
              <div class="text-xs text-white/60">I+D & calidad</div>
              <img src="" alt="">
            </div>
          </div>
        </div>

        <div class="rounded-xl border border-white/10 bg-white/5 p-6 text-white/90 backdrop-blur">
          <h2 class="text-xl font-medium">Filosofía</h2>
          <p class="mt-2 text-white/70">
            Calidad, consistencia y creatividad. Lotes pequeños, trazabilidad completa y cero atajos.
          </p>
          <div class="mt-4 flex flex-wrap gap-2">
            <span class="chip">Transparencia</span>
            <span class="chip">Circularidad</span>
            <span class="chip">Carbono consciente</span>
          </div>
        </div>
      </div>
    </section>

    <!-- TIMELINE GLOW -->
    <section class="mx-auto mt-12 max-w-4xl">
      <h2 class="mb-6 text-2xl font-semibold text-white">Trayectoria</h2>
      <ol class="relative border-l border-white/15 pl-6">
        {[
          { y: "2019", t: "Primera cocción piloto." },
          { y: "2021", t: "Línea base: Amber, IPA y Stout." },
          { y: "2023", t: "Ediciones colaborativas y limitadas." },
          { y: "2024", t: "Taproom y expansión de distribución." },
        ].map((nodo) => (
          <li class="timeline-node">
            <span class="timeline-dot"></span>
            <div class="timeline-card">
              <div class="text-amber-300/90 text-sm font-medium">{nodo.y}</div>
              <div class="text-white/90">{nodo.t}</div>
            </div>
          </li>
        ))}
      </ol>
    </section>

    <!-- CTA -->
    <section class="mx-auto my-12 max-w-5xl rounded-xl border border-white/10 bg-white/5 p-6 text-center text-white/90 backdrop-blur">
      <h3 class="text-xl font-medium">¿Hablemos?</h3>
      <p class="mt-1 text-white/70">Prensa, colaboraciones y distribución.</p>
      <a href="mailto:hola@tu-marca.cl"
         class="btn-cta mt-4 inline-flex items-center gap-2">
        Escríbenos
        <svg viewBox="0 0 24 24" class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M13 5l7 7-7 7"/>
        </svg>
      </a>
    </section>
  </article>

  <style is:global>
    /* Chips / tags */
    .chip {
      @apply inline-flex items-center rounded-full border border-white/15 bg-white/10 px-2.5 py-1 text-xs text-white/80 backdrop-blur;
    }

    /* Fundadores (efecto polaroid + borde dinámico) */
    .founder {
      @apply rounded-xl border border-white/10 bg-white/5 p-3 backdrop-blur text-center;
      transition: transform .25s ease, box-shadow .25s ease, border-color .25s ease;
    }
    .founder:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 30px rgba(0,0,0,.25);
      border-color: rgba(245,158,11,.35);
    }
    .founder-img {
      @apply h-24 w-full rounded-lg bg-center bg-cover;
      box-shadow: inset 0 0 0 1px rgba(255,255,255,.08);
    }

    /* Timeline con glow */
    .timeline-node {
      @apply mb-6;
      position: relative;
    }
    .timeline-dot {
      position: absolute; left: -0.59rem; top: .35rem;
      width: .65rem; height: .65rem; border-radius: 9999px;
      background: radial-gradient(circle at 50% 50%, #f59e0b, #7c3aed 60%, transparent 70%);
      box-shadow: 0 0 22px rgba(245,158,11,.5);
    }
    .timeline-card {
      @apply rounded-lg border border-white/10 bg-white/5 px-4 py-3 backdrop-blur;
    }

    /* Botón CTA */
    .btn-cta {
      @apply rounded-lg border border-amber-400/50 bg-amber-400/15 px-4 py-2 text-amber-100;
      box-shadow: 0 10px 25px rgba(245,158,11,.18), inset 0 1px 0 rgba(255,255,255,.06);
      transition: transform .2s ease, box-shadow .2s ease, background-color .2s ease;
    }
    .btn-cta:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 30px rgba(245,158,11,.25), inset 0 1px 0 rgba(255,255,255,.1);
      background-color: rgba(245,158,11,.22);
    }

    /* Prose base (si usas @tailwind/typography) */
    .prose { color: inherit; }
  </style>
</BaseLayout>
